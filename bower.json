{
  "name": "jquery.closestChildren",
  "version": "0.0.1",
  "authors": ["Adam Cook aka onecreative"],
  "description": "An opposite of `closest()`: find the nearest child that matches a selector, and optionally find the first matching children on each branch.",
  "main": "jquery.closestChildren.js",
  "keywords": [
    "jquery.closestchildren",
    "jquery-closestchildren",
    "closestchild",
    "closest child",
    "closest",
    "nearest",
    "nearestchild",
    "nearest child"
  ],
  "license": "MIT",
  "dependencies": {
    "jquery": ">=1.5"
  }
}
